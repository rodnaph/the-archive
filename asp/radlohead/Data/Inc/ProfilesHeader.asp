<%

  leftNode = 16;

  // field types for profile
  var textfields = new Array( 'boardName', 'realName', 'from', 'gender', 'dateOfBirth', 'image', 'website', 'email' );
  var textareas = new Array( 'stuff' );

  // create field array
  var fields = new Array();
  fields = fields.concat( textfields, textareas );

%>